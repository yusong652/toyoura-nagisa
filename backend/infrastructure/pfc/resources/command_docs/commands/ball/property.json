{
  "command": "ball property",
  "category": "ball",
  "description": "Assign ball surface properties used in contact models. Properties are surface conditions (e.g., stiffness, friction) distinct from ball attributes (position, velocity). Property names depend on the active contact model. See Contact Models documentation for available properties per model.",

  "syntax": "ball property <s a> <s1 a1> ... <range>",

  "parameters": [
    {
      "name": "s a",
      "type": "property name-value pairs",
      "required": true,
      "description": "Surface property name (string) and value. Properties depend on contact model (linear: kn/ks, hertz: hz_kn, flat joint: fj_kn/fj_coh, etc.). Can specify multiple name-value pairs."
    },
    {
      "name": "range",
      "type": "range specification",
      "required": false,
      "description": "Range of balls to assign properties (default: all balls)"
    }
  ],

  "examples": [
    {
      "command": "ball property 'kn' 1e8 'ks' 5e7",
      "description": "Set normal and shear stiffness (linear contact model)"
    },
    {
      "command": "ball property 'fric' 0.5",
      "description": "Set friction coefficient for all balls"
    },
    {
      "command": "ball property 'pb_kn' 1e9 'pb_ks' 5e8 'pb_coh' 1e6 range group 'bonded'",
      "description": "Set parallel bond properties (linear parallel bond model) for specific group"
    },
    {
      "command": "ball property 'hz_kn' 5e8 range id 1 100",
      "description": "Set Hertz normal stiffness for balls 1-100"
    }
  ],

  "python_sdk_alternative": {
    "available": true,
    "workaround": "Ball.set_prop(property_name, value) - requires iteration: for b in itasca.ball.list(): b.set_prop('kn', 1e8)"
  }
}
