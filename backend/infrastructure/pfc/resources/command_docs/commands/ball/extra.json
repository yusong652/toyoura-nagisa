{
  "command": "ball extra",
  "category": "ball",
  "description": "Set ball extra variables. The extra variable can be of any type (number, vector, string) and is associated with the index as if stored in an array.",

  "syntax": "ball extra <i> <value> <range>",

  "parameters": [
    {
      "name": "i",
      "type": "integer",
      "required": true,
      "description": "Extra variable index (slot number)"
    },
    {
      "name": "value",
      "type": "any",
      "required": true,
      "description": "Value to set (can be number, vector, or string)"
    },
    {
      "name": "range",
      "type": "range specification",
      "required": false,
      "description": "Range of balls to set extra variable (default: all balls)"
    }
  ],

  "examples": [
    {
      "command": "ball extra 1 0 range position-z [z1] [z2]",
      "description": "Set extra variable at index 1 to 0 for balls in z-coordinate range"
    },
    {
      "command": "ball extra 5 (90,100,110)",
      "description": "Set extra variable at index 5 to vector (90,100,110) for all balls"
    },
    {
      "command": "ball extra 1 'Fred'",
      "description": "Set extra variable at index 1 to string 'Fred' for all balls"
    },
    {
      "command": "ball extra 3 1.5 range group 'layer1'",
      "description": "Set extra variable at index 3 to 1.5 for balls in group 'layer1'"
    }
  ],

  "python_sdk_alternative": {
    "available": true,
    "workaround": "Ball.set_extra(slot, value) - requires iteration: for b in itasca.ball.list(): b.set_extra(i, value)"
  }
}
