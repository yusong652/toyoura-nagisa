# Shared Prompt Config

`config/prompts/` is the preferred shared location for prompt templates.

Current runtime fallback order:

1. `config/prompts/`
2. `packages/backend/config/prompts/`

`config/prompts/` is now the primary shared location.
