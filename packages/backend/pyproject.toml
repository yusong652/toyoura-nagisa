[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "toyoura-nagisa-backend"
version = "0.1.0"
description = "toyoura-nagisa FastAPI backend"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["backend"]
